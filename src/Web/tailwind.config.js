module.exports = {
    theme: {
        extend: {},
    },
    variants: {
        rounded: ['first', 'last'],
        borderRadius: ['first', 'last'],
        borderWidth: ['focus'],
        padding: ['focus'],
        textColor: ['responsive', 'hover', 'focus', 'group-hover'],
    },
    plugins: [],
}
