name: multiple-services
description: Example of a server talking to an API, both running in clud
entryPoint: web
services:
  - name: web
    dockerfile: ./Web/dockerfile
    httpPort: 80
  - name: api
    dockerfile: ./Api/dockerfile
    httpPort: 80
